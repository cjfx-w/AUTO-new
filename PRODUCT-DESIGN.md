# AUTO：Pinterest 矩阵视频发布工具产品设计文档

## 1. 项目目标

AUTO 是一个面向运营人员的 Pinterest 视频矩阵发布工具。首期通过比特浏览器隔离账号和代理环境，使用 Playwright 控制真实浏览器页面，完成产品视频的批量发布。

首期只支持 Pinterest 视频 Pin，后续再增加图片、其他 Pin 类型以及 Instagram、X 等平台。

核心目标：

- 让运营人员通过导入包批量准备产品、视频和文案。
- 自动生成“产品 × 视频 × 账号 × Board”的发布任务。
- 支持多账号有限并行、单账号严格串行。
- 发布结果必须有证据，不以点击按钮直接判定成功。
- 结果不明确时暂停，不自动重复上传。
- 记录每个视频在每个账号上的发布状态，避免重复发布和漏发布。
- 发生异常时可人工核验并安全恢复。

旧项目 `D:\work\fb` 只作为业务经验和错误案例参考，不复制其页面结构、Facebook 专用代码或状态逻辑。

## 2. 首期范围

### 包含

- 比特浏览器窗口读取和连接。
- Pinterest 账号和 Board 管理。
- Excel 导入包。
- 产品及多个视频素材管理。
- 视频、标题、描述、链接、账号和 Board 的矩阵任务生成。
- 视频 Pin 浏览器发布。
- 导入批次预览和确认。
- 单账号串行和多账号有限并行。
- 发布截图、页面 URL、页面状态和错误证据保存。
- 发布账本、防重复和失败恢复。
- 运营人员可理解的问题处理页面。

### 暂不包含

- Pinterest 官方 API。
- 图片 Pin、Carousel、Idea Pin。
- 自动创建 Board。
- 自动点赞、关注、评论养号或绕过验证码、身份验证和账号限制。
- Instagram、X、Facebook 等其他平台。

## 3. 产品与素材导入

员工使用一个导入包，不需要在软件中逐条新建任务：

```text
Pinterest导入包.zip
├── pins.xlsx
└── media/
    ├── P001-V01.mp4
    ├── P001-V02.mp4
    └── P001-V03.mp4
```

Excel 每行描述一个视频素材或一个发布计划，首期字段包括：

- `product_id`：产品编号。
- `product_name`：产品名称。
- `asset_id`：视频编号，例如 `P001-V01`。
- `filename`：导入包 `media/` 中的视频文件名。
- `cover_filename`：可选封面文件名。
- `title`：Pin 标题。
- `description`：Pin 描述，可包含标签。
- `product_url`：产品或独立站链接。
- `account_group`：目标账号组。
- `board`：目标 Board 名称或已绑定 Board ID。
- 批次确认后立即进入任务队列，不设置单独发布时间。

导入步骤：

```text
拖入 ZIP
→ 解析 Excel
→ 校验素材文件和字段
→ 计算视频哈希、时长和大小
→ 展开账号矩阵
→ 按批次展示预览
→ 员工修改并确认批次
→ 生成可执行任务
```

不使用员工电脑的绝对路径。素材身份由 `asset_id`、文件名和内容哈希共同确认；文件改名后仍可识别，内容相同但文件名不同的视频会被识别为重复素材。

## 4. 产品、视频和矩阵任务

数据关系：

```text
产品
└── 视频素材
    └── 发布任务
        ├── Pinterest账号
        ├── Board
        └── 发布尝试
```

例如：

```text
P001-V01 → 账号 A
P001-V01 → 账号 B
P001-V02 → 账号 A
P001-V02 → 账号 B
```

系统将其展开为独立任务。相同产品的不同视频可以发给同一个账号；相同视频发给不同账号不算重复。

内部实体：

- `products`：产品。
- `assets`：产品视频和封面。
- `accounts`：Pinterest 账号及其比特窗口。
- `boards`：账号可用的 Board。
- `publish_tasks`：发布任务。
- `publish_attempts`：每次实际执行尝试。
- `publication_ledger`：最终确认的发布记录。
- `evidence`：截图、URL、页面状态和验证信息。
- `manual_decisions`：人工确认或恢复记录。

## 5. Pinterest 视频发布流程

Pinterest 视频 Pin 的页面流程为：

```text
检查账号登录
→ 打开新建 Pin 页面
→ 上传视频
→ 等待视频处理完成
→ 选择封面
→ 填写标题
→ 填写描述和标签
→ 填写产品链接
→ 选择 Board
→ 点击 Publish
→ 等待并收集发布结果
→ 保存证据和 Pin URL
```

Pinterest 官方流程包含视频、封面、标题、链接、描述和 Board 等信息；视频上传后可能需要额外处理时间。参考：[Pinterest 创建图片或视频 Pin](https://help.pinterest.com/en/article/create-a-pin-from-an-image-or-video)。

发布适配器统一提供以下能力：

```text
checkAccount()
openComposer()
uploadVideo()
waitForVideoReady()
selectCover()
fillTitle()
fillDescription()
fillLink()
selectBoard()
publish()
collectEvidence()
verifyPublished()
classifyResult()
```

页面适配器只负责页面动作和结果报告，不直接修改核心数据库状态。

## 6. 状态、证据和恢复

任务状态：

```text
导入预览
已确认并排队
执行中
已发布
失败可重试
结果待核验
需要人工处理
已跳过
```

每个步骤都遵循：

```text
输入 → 执行动作 → 收集证据 → 判定结果 → 进入下一状态或恢复流程
```

不为每一种错误编写独立流程，而是将结果归类为：

```text
SUCCESS
RETRYABLE
UNCERTAIN
BLOCKED
INVALID_INPUT
FATAL
```

恢复规则：

- 发布前的网络、页面加载或窗口连接失败：允许重试当前步骤。
- 视频上传失败：重新创建发布页面后重新上传。
- 已点击 Publish 但没有确认结果：进入“结果待核验”，禁止自动重传。
- 验证码、身份验证、账号限制：暂停账号并等待人工处理。
- 明确确认未发布：创建新的发布尝试。
- 明确确认已发布：写入账本并结束任务。

问题中心提供：

```text
[确认已发布]
[确认未发布并重试]
[修改任务后重试]
[暂不处理]
```

人工恢复前，系统重新检查账号、Board、发布账本、素材哈希和当前页面，防止暂停期间状态发生变化。

## 7. 防重复规则

默认唯一键：

```text
platform
+ account_id
+ board_id
+ product_id
+ asset_id
+ content_version
```

相同视频发布到不同账号允许；相同视频重复发布到同一账号和 Board 默认阻止。

标题、描述或链接发生有效变化时生成新的 `content_version`，但不会改变原始视频的 `asset_id`。

防重复账本和页面显示记录分离：清空历史显示不删除防重复账本。

## 8. 前端和运营流程

主导航：

1. 工作台
2. 导入与素材
3. 发布中
4. 发布记录
5. 异常处理
6. 账号管理
7. 管理员设置

工作台只展示运营人员需要的内容：

```text
待确认批次
今日待发布
已发布
结果待核验
需要处理的问题
账号异常
```

普通运营人员不接触 API 地址、数据库路径、Playwright、CDP、并发实现和日志文件路径。比特浏览器、代理、窗口并发、数据目录和诊断设置放入管理员区域。

每条任务必须明确显示：

```text
产品 / 视频 / Pinterest账号 / Board / 发布时间 / 当前状态 / 下一步操作
```

## 9. 文件、日志和账本存储

默认保存到运行 AUTO 的电脑：

```text
D:\work\AUTO\data\
├── auto.db
├── assets\
├── evidence\
│   └── task-id\
│       ├── before-publish.png
│       ├── after-publish.png
│       ├── page.html
│       └── metadata.json
├── logs\
└── exports\
```

数据库保存结构化状态和证据索引；截图、HTML、日志和导出文件保存为外部文件。后续多电脑协作时再迁移到中心数据库或 NAS。

## 10. 技术架构

```text
Electron
├── 运营工作台
├── SQLite 数据层
├── XState 任务状态机
├── 本地任务调度器
├── Playwright 自动化层
├── BitBrowser CDP 连接层
└── 证据与日志系统
```

推荐使用 [XState](https://github.com/statelyai/xstate) 管理状态转换，使用 [SheetJS](https://github.com/SheetJS/sheetjs) 解析 Excel。Pinterest 的 [PinterestBulkPostBot](https://github.com/SoCloseSociety/PinterestBulkPostBot) 只借鉴其 CSV/批量任务思想，不直接复制 Selenium 发布实现。

第一版不引入 Temporal 或 BullMQ：Temporal 需要额外服务，BullMQ 通常需要 Redis，都会增加单机桌面工具的部署和维护成本。未来扩展到多电脑、多 Worker 时再评估。

## 11. 旧 FB 工具错误的规避

AUTO 明确不采用以下旧模式：

- 不复制 Facebook 专用页面和状态字段。
- 不用固定等待时间作为主要判断依据。
- 不把点击 Publish 直接当作成功。
- 不根据文件夹扫描结果推断发布状态。
- 不使用文件名作为唯一素材身份。
- 不让页面适配器直接修改任务状态。
- 不把任务、尝试、账本和证据混在一张表里。
- 不允许结果不明时自动重复上传。
- 不让运营人员操作技术配置。
- 不把所有异常都归类为 `UNKNOWN`。
- 不在缺少依赖锁定和测试的情况下交付。

## 12. 第一阶段验收标准

以下闭环全部通过后，才增加新的 Pin 类型或平台：

```text
导入一个产品和多个视频
→ 绑定多个 Pinterest 账号和 Board
→ 正确生成产品/视频/账号矩阵任务
→ 员工预览并确认任务
→ 通过比特浏览器发布视频
→ 保存截图、页面状态和 Pin URL
→ 准确显示每个视频/账号的状态
→ 模拟网络和页面失败后可恢复
→ 发布结果不确定时禁止重复上传
→ 人工确认后可安全恢复
→ 程序重启后任务和账本不丢失
```

开发期间必须为每次代码改动补充或更新测试，并创建对应 Git commit。

## 13. 第一版必须闭环的产品要求

第一版不追求高并发，必须优先保证以下四项：

1. **导入预览确认**：导入、校验和矩阵展开后，任务直接进入批次预览，运营人员可修改标题、描述、链接和 Board，点击确认后才进入执行队列。
2. **结果核验中心**：发布结果不明确时，展示发布尝试编号、最后执行步骤、截图、页面 URL、页面状态和推荐操作。
3. **独立异常中心**：集中处理结果待核验、账号验证、素材字段错误和可重试任务；异常只暂停受影响的任务或账号。
4. **防重复锁定**：发布尝试一旦点击 Publish，即使结果未知，也要锁定默认唯一键，禁止自动重传。只有人工确认未发布后，才能创建新的发布尝试。

素材和 Board 规则：

- 一个视频素材对应一个独立 Pin 任务。网页端即使一次选择多个视频，也按多个独立 Pin 处理，不把多个视频合并成一个任务。
- 每个 Pinterest 账号的 Board 由运营人员提前在账号中创建。
- 导入清单填写 Board 名称；AUTO 连接账号后读取现有 Board 并校验匹配。
- Board 不存在、名称不匹配或账号无法读取 Board 时，任务停留在导入预览，不自动创建 Board。

第一版建议范围：单账号严格串行，多账号有限并行；不做高并发、不自动绕过验证码或身份验证、不在结果未知时重试上传。

## 14. AUTO 与比特浏览器协同边界

比特浏览器负责账号隔离和浏览器环境，AUTO 负责任务、调度、页面动作、证据和账本。协同流程如下：

```text
AUTO 选择账号任务
→ 连接该账号对应的比特浏览器窗口/CDP
→ 检查登录状态和当前页面
→ Playwright 上传当前任务的单个视频
→ 等待视频处理完成并选择已校验的 Board
→ 填写标题、描述、链接并发布
→ 收集截图、页面状态和 Pin URL
→ AUTO 分类结果并更新任务、尝试和发布账本
```

必须遵守以下安全边界：

- 账号、代理、比特浏览器窗口必须一一绑定，禁止串号。
- 连接断开、登录过期、验证码、身份验证和账号限制时，立即暂停，不自动绕过。
- 页面适配器只返回动作结果和证据，不直接修改核心任务状态。
- 点击 Publish 不等于成功，必须经过页面结果验证。
- 运行日志、截图、页面 HTML、Cookie 和本地数据库需要限制访问权限并定期备份。

账号接入流程：

```text
在比特浏览器创建窗口
→ 为每个窗口配置代理
→ 每个窗口登录一个 Pinterest 账号
→ AUTO 连接比特浏览器并读取窗口列表
→ 员工选择已登录窗口并绑定 Pinterest 账号
→ 校验登录状态和 Board 列表
→ 保存账号、窗口、代理区域和 Board 映射
```

AUTO 不导入 Pinterest 密码，不复制 Cookie，不负责购买或切换代理。账号登录、代理配置和验证都在比特浏览器窗口中完成。

## 15. 分阶段产品交付

AUTO 不一次性开放全部能力，按以下顺序逐步交付：

1. **界面演示阶段**：只验证工作台、导入预览、队列、账号和异常流程，不连接真实账号。
2. **比特连接阶段**：只连接本机 BitBrowser，读取窗口列表，不能发布。
3. **账号准备阶段**：绑定 Pinterest 账号，同步 Board，处理登录和 Board 异常。
4. **导入预览阶段**：导入完成后仍停留在“导入与素材”页面，按批次展示任务；运营人员可修改 Board、标题、描述和链接，每批点击确认后分别进入队列。
5. **单任务预演阶段**：一个账号执行一个视频，停在 Publish 前供人工检查。
6. **单任务发布阶段**：允许一次真实发布，保存 Pin URL 和证据。
7. **批量执行阶段**：增加多账号有限并行、账号串行和立即执行队列。
8. **恢复完善阶段**：增加断线恢复、结果待核验、程序重启恢复。

每个阶段必须先通过验收，再进入下一个阶段。运营界面只展示：

```text
正常
处理中
发布失败，可重试
需要人工确认
```

CDP、Playwright、选择器、HTML 和诊断 Trace 只放在管理员诊断区域，不展示给普通运营人员。产品上不单独设置“任务审核”模块，导入预览中的“确认这批任务”就是进入队列前的确认动作。

## 15. 运营人员完整操作流程

```text
一、准备账号
在比特浏览器创建独立窗口
→ 每个窗口配置稳定代理
→ 每个窗口登录一个 Pinterest 账号
→ 在每个账号中提前创建 Board

二、连接账号
AUTO → 账号管理 → 连接比特浏览器
→ 读取窗口列表
→ 选择 Pinterest 账号
→ 校验登录状态和 Board
→ 保存账号绑定

三、导入内容
选择导入文件夹
→ 读取 pins.xlsx 和 media/
→ 校验字段、视频文件和内容哈希
→ 每个视频生成一个独立 Pin 任务
→ 按产品、视频、账号和 Board 展开矩阵

四、导入预览
检查标题、描述、链接、账号和 Board
→ 修正缺失或不匹配的 Board
→ 检查防重复结果
→ 运营人员确认这批任务后进入任务队列

五、执行发布
连接对应比特浏览器窗口
→ 检查登录状态
→ 上传单个视频
→ 等待视频处理完成
→ 选择已校验 Board
→ 填写内容并发布
→ 保存截图、页面状态和 Pin URL

六、处理结果
明确成功 → 写入发布账本
明确失败 → 按规则重试
结果不明 → 锁定任务并进入异常中心
账号验证或限制 → 暂停账号，人工处理后重新校验
```
