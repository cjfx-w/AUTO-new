# AUTO 产品设计 v1

## 目标

运营人员把产品表格和视频导入 AUTO，按批次确认后，系统通过对应 BitBrowser 窗口发布 Pinterest 视频 Pin。

## 运营流程

```text
连接 BitBrowser 账号
→ 导入产品文件夹
→ 读取表格和视频
→ 在“导入与素材”页面按批次预览
→ 修改需要修改的内容
→ 确认本批并立即加入任务队列
→ 系统自动发布
→ 查看结果或处理异常
```

## 导入内容

每个视频对应一个 Pin 任务。表格可包含：

```text
产品编号、产品名称、视频编号、文件名
标题、描述、产品链接、账号组、Board
```

运营人员不需要重新填写已经存在于表格中的内容。Board 由每个 Pinterest 账号提前创建，AUTO 只负责匹配和选择。

## 任务队列规则

- 同一账号一次只发布一个任务。
- 同一 BitBrowser 窗口一次只执行一个任务。
- 不同账号允许有限并行。
- 确认批次后立即进入队列，不设置发布时间。
- 一个视频发布到多个账号，会生成多个独立任务。
- 发布前失败可以重试。
- Publish 后结果不明确，暂停并人工核验，不自动重传。

## 账号与 Board

```text
BitBrowser 窗口 → Pinterest 账号 → 该账号的 Board
```

不同账号可以有同名 Board，但实际仍是不同 Board，AUTO 按账号分别匹配。

## 运营人员看到的状态

```text
正常
处理中
发布失败，可重试
需要人工确认
```

技术错误、CDP、Playwright、选择器和诊断文件只放在管理员区域。

## 第一版范围

包含：BitBrowser 连接、账号绑定、Board 同步、导入预览、批次确认、任务队列、Pinterest 视频 Pin 发布、截图和 Pin URL、异常恢复、防重复。

不包含：代理购买或管理、自动创建 BitBrowser 窗口、自动点赞/关注/评论、绕过验证、其他平台和其他 Pin 类型。

## 验收

```text
连接账号
→ 导入一个产品和多个视频
→ 分批预览并确认
→ 生成正确任务
→ 发布一个 Pin
→ 保存结果和证据
→ 模拟失败后安全暂停或重试
```
